import { EditOptionDirective } from './edit-option.directive';

describe('EditDictionaryOptionDirective', () => {
  it('should create an instance', () => {
    const directive = new EditOptionDirective();
    expect(directive).toBeTruthy();
  });
});
